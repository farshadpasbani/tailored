---
id: "011a"
title: Connect status-aware corpus and editorial authority
status: done
depends_on: ["010"]
acceptance:
  - Only exact-hash approved/submitted corpus documents are read and bound; all non-final statuses are excluded before path resolution or byte access
  - The physical pack-vault trust base, descriptors and eligible members are non-symlink and contained; path identity excludes only the current artefact while a distinct byte twin remains eligible
  - Strategy opening, argument, anchor facts and selected projects produce stable advisories without inventing missing fields or auto-passing semantic evidence altitude
  - Distinctness, density, selection, self-reference and natural-language findings are live in verify-pack receipts and failed human-resolved advisories remain ok false
  - The full read-only corpus reproduces the corrected eligible denominator and collision classes without private bytes or names entering Git
files:
  - src/verify/trust.ts
  - src/strategy/schema.ts
  - src/gates/editorial.ts
  - src/verify/pack.ts
---

## Boundary

This unit deliberately excludes page-margin, font and line-height authority. It preserves the
existing accessibility finding and policy shape byte-for-byte; replacement `011c` now owns that
joint after retired 011b failed final review. The result is nevertheless live: every new corpus, strategy and
editorial rule runs inside the existing `verify-pack` transaction and is bound into receipt v1.

Rejected `codex/011-editorial-advisories` commits are an idea/test quarry only. Reimplement from
current main or reconstruct individual hunks with a traceable diff; do not cherry-pick the branch.

## Privacy and human authority

Field records use only generic labels such as `field-pack-a` and aggregate counts/digests. No agent
may create an attestation, waiver, approval, publication or submission.

## Execution record — 2026-07-14

- Base: `c7557866c0b1390e100237e57445992f0f4bbd5a`; implementation: `2520a4f`. The
  rejected 011 branch contributed no ancestry or commits.
- Necessity: reused descriptor, immutable snapshot, distinct/impact, finding disposition, receipt
  and atomic exposure machinery. New behavior is limited to corpus trust selection, one strategy
  schema/evaluator, one impact-composing editorial evaluator and verifier wiring.
- Scope: 4/4 allowed production files, 216 added lines (ceiling 300); 3/4 allowed test files,
  229 added lines (ceiling 360); this status record is the only changed record.
- Corpus evidence covers all six non-final lifecycle values before hostile-path access; eligible
  missing, unreadable, stale, escape, symlink base/descriptor/parent/member, malformed nesting,
  cycle, duplicate/case collision and permutation cases; physical current-path exclusion retains a
  separate byte twin.
- Strategy/editorial evidence covers complete and legacy records, blank/empty/duplicate/unknown/
  select-all IDs, fact quality, mandatory human semantic-altitude review, density, weak phrasing,
  skills, dates, repetition, rhetorical contrast, person consistency and naturalness.
- Review remediation binds each corpus descriptor/member validation and byte read to one no-follow
  file descriptor with matching pre-open/post-open device and inode identity. Deterministic root,
  nested, symlink, regular-replacement and descriptor-close tests pass; an independent 1000-attempt
  regular/symlink race accepted zero outside-byte snapshots.
- Targeted gate: 31 passed. Distinct/impact/production regression: 52 passed. Privacy: 6 passed.
  Full suite: 477 passed, 3 skipped. Build passed. Dry pack: 91 files, 696435 bytes packed,
  1186284 bytes unpacked.
- `field-corpus`: inventory 8 submitted packs matched the supplied manifest; 16 eligible documents,
  8 covers, 0/8 cover-to-cover collisions and 3/8 cover-to-all collisions. Before/after source
  consumed-source manifest digest `51b827dc…8cb97b` was byte-identical before/after with zero
  mutations; the supplied corpus binding digest was `5e209f62…947c26`.
- Accessibility, policy, Chrome, claim integrity, receipt, CLI and all 012 paths have zero diff.
  No deviation, private repair, human resolution, approval, publication or submission occurred.
- Independent final review on `6c71a00ec74ae4e61c1d77db9525ffb6865b9679` returned
  `COMPLIANT — APPROVE` from both the source/correctness and structure/simplicity lenses. The
  reviewed tip was fast-forwarded unchanged to `main`; replacement `011c` is dependency-ready.
